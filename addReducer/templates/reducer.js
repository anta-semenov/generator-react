import * as actionTypes from '<%= actionTypesPath %>'

const <%= reducerName %> = (state, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default <%= reducerName %>

/*
 Selectors
*/
